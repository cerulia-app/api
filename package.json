{
  "name": "@cerulia/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/entrypoints/bun.ts",
    "build": "tsc --noEmit",
    "test": "bun test",
    "check": "tsc --noEmit && bun test"
  },
  "dependencies": {
    "hono": "^4.10.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "typescript": "^6.0.3"
  }
}